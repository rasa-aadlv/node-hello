{
  "git": {
    "requireBranch": "release/*",
    "commitMessage": "Release ${version}",
    "requireCleanWorkingDir": true
  },
  "hooks": {
    "after:bump": "npx auto-changelog -p"
  },
  "github": {
    "release": true,
    "releaseNotes":"git log --pretty=oneline `git tag --sort=-committerdate | head -1`...`git tag --sort=-committerdate | head -2 | tail -1`", 
    "autoGenerate": true
  },
  "npm": {
    "publish": false
  }
}
